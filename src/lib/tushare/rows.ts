export type DailyBasicRow = [
  string,
  string,
  number | null,
  number | null,
  number | null,
  number | null,
  number | null,
]

export type IndexDailyBasicRow = [
  string,
  string,
  number | null,
]

export type DailyPcfRow = [
  string,
  string,
  number | null,
]

export type FinaIndicatorRow = [
  string,
  string,
  number | null,
  number | null,
  number | null,
  number | null,
  number | null,
  number | null,
  number | null,
  number | null,
  number | null,
]

export type CashflowRow = [
  string,
  string,
  number | null,
  number | null,
  number | null,
]

export type StockBasicRow = [
  string,
  string,
  string | null,
  string | null,
  string | null,
]

export type BalanceSheetRow = [
  string,
  string,
  number | null,
  number | null,
  number | null,
  number | null,
  number | null,
  number | null,
  number | null,
]

export type IncomeRow = [
  string,
  string,
  number | null,
]

export type StkFactorRow = [
  string,
  string,
  number | null,
]

export type CnCpiRow = [
  string | null,
  number | null,
  number | null,
]

export type OptionalRiskRow = Array<string | number | null>